import{useEffect,useRef}from'react';
export function ConfirmDialog({open,title,children,busy,onCancel,onConfirm}:{open:boolean;title:string;children:React.ReactNode;busy:boolean;onCancel:()=>void;onConfirm:()=>void}){const ref=useRef<HTMLDialogElement>(null);useEffect(()=>{if(open&&!ref.current?.open)ref.current?.showModal();if(!open&&ref.current?.open)ref.current.close()},[open]);return <dialog ref={ref} onCancel={e=>{e.preventDefault();if(!busy)onCancel()}} aria-labelledby="dialog-title"><h2 id="dialog-title">{title}</h2><p>{children}</p><div className="actions"><button className="ghost" disabled={busy} onClick={onCancel}>Quay lại</button><button disabled={busy} onClick={onConfirm}>{busy?'Đang xử lý…':'Xác nhận'}</button></div></dialog>}
