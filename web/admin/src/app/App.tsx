import{Navigate,NavLink,Outlet,Route,Routes,useNavigate}from'react-router-dom';import{useAuth}from'../auth/AuthContext';import{LoginPage}from'../auth/LoginPage';import{NotificationList}from'../notifications/NotificationList';import{NotificationDetail}from'../notifications/NotificationDetail';
function Guard(){const a=useAuth();if(!a.ready)return <main className="center"><div className="spinner" aria-label="Đang tải"/></main>;return a.authenticated?<Outlet/>:<Navigate to="/login" replace/>}
function Shell(){const a=useAuth(),nav=useNavigate();return <div className="shell"><aside><div className="brand"><span>NT</span><div>Notification<small>Operations console</small></div></div><nav aria-label="Điều hướng chính"><NavLink to="/notifications">Thông báo</NavLink><span aria-disabled>Thiết bị <em>Sắp có</em></span><span aria-disabled>Template <em>Sắp có</em></span><span aria-disabled>Sender <em>Sắp có</em></span></nav><button className="ghost logout" onClick={async()=>{await a.logout();nav('/login')}}>Đăng xuất</button></aside><main><Outlet/></main></div>}
export function App(){return <Routes><Route path="/login" element={<LoginPage/>}/><Route element={<Guard/>}><Route element={<Shell/>}><Route path="/notifications" element={<NotificationList/>}/><Route path="/notifications/:id" element={<NotificationDetail/>}/><Route index element={<Navigate to="/notifications" replace/>}/></Route></Route><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}
